<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.thinkgem.jeesite.modules.test.dao.GeoDistributionDao">
    
	<sql id="geoDistributionColumns">
		a.id AS "id",
		a.name AS "name"
	</sql>
	
	<sql id="geoDistributionJoins">
	</sql>
    
	<select id="get" resultType="GeoDistribution">
		SELECT 
			<include refid="geoDistributionColumns"/>
		FROM geo_distribution a
		<include refid="geoDistributionJoins"/>
		WHERE a.id = #{id}
	</select>
	
	<select id="findList" resultType="GeoDistribution">
		SELECT 
			<include refid="geoDistributionColumns"/>
		FROM geo_distribution a
		<include refid="geoDistributionJoins"/>
		<where>
			
			<if test="id != null and id != ''">
				AND a.id = #{id}
			</if>
			<if test="name != null and name != ''">
				AND a.name LIKE 
					<if test="dbName == 'oracle'">'%'||#{name}||'%'</if>
					<if test="dbName == 'mssql'">'%'+#{name}+'%'</if>
					<if test="dbName == 'mysql'">concat('%',#{name},'%')</if>
			</if>
		</where>
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
			</otherwise>
		</choose>
	</select>
	
	<select id="findAllList" resultType="GeoDistribution">
		SELECT 
			<include refid="geoDistributionColumns"/>
		FROM geo_distribution a
		<include refid="geoDistributionJoins"/>
		<where>
			
		</where>		
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
			</otherwise>
		</choose>
	</select>
	
	<insert id="insert">
		INSERT INTO geo_distribution(
			id,
			name
		) VALUES (
			#{id},
			#{name}
		)
	</insert>
	
	<update id="update">
		UPDATE geo_distribution SET 	
			name = #{name}
		WHERE id = #{id}
	</update>
	
	<update id="delete">
		DELETE FROM geo_distribution
		WHERE id = #{id}
	</update>
	
</mapper>