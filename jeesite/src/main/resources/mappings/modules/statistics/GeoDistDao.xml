<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.thinkgem.jeesite.modules.statistics.dao.GeoDistDao">
    
	<sql id="geoDistColumns">
		a.id AS "id",
		a.name AS "name"
	</sql>
	
	<sql id="geoDistJoins">
	</sql>
    
	<select id="get" resultType="GeoDist">
		SELECT 
			<include refid="geoDistColumns"/>
		FROM geo_dist a
		<include refid="geoDistJoins"/>
		WHERE a.id = #{id}
	</select>
	
	<select id="findList" resultType="GeoDist">
		SELECT 
			<include refid="geoDistColumns"/>
		FROM geo_dist a
		<include refid="geoDistJoins"/>
		<where>
			
			<if test="id != null and id != ''">
				AND a.id = #{id}
			</if>
			<if test="name != null and name != ''">
				AND a.name LIKE 
					<if test="dbName == 'oracle'">'%'||#{name}||'%'</if>
					<if test="dbName == 'mssql'">'%'+#{name}+'%'</if>
					<if test="dbName == 'mysql'">concat('%',#{name},'%')</if>
			</if>
		</where>
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
			</otherwise>
		</choose>
	</select>
	
	<select id="findAllList" resultType="GeoDist">
		SELECT 
			<include refid="geoDistColumns"/>
		FROM geo_dist a
		<include refid="geoDistJoins"/>
		<where>
			
		</where>		
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
			</otherwise>
		</choose>
	</select>
	
	<insert id="insert">
		INSERT INTO geo_dist(
			id,
			name
		) VALUES (
			#{id},
			#{name}
		)
	</insert>
	
	<update id="update">
		UPDATE geo_dist SET 	
			name = #{name}
		WHERE id = #{id}
	</update>
	
	<update id="delete">
		DELETE FROM geo_dist
		WHERE id = #{id}
	</update>
	
	
	
	<!-- baidu TOP10 地域分布 -->
	<select id="geoDistribution" resultType="hashmap">	
		select b.area, COUNT(b.url) num
		from t_d_counts b
		where b.siteid='1111'
		group by b.area
		order by num DESC
	</select>
	
	
	
</mapper>